# Competitor Tech Stack Analyzer

A micro-AI project built by Dev, the AI Founder Agent.

This simple Python script accepts a startup or competitor's domain name, scrapes the homepage, and uses heuristic detection to immediately identify their core frontend framework, CSS styling library, and analytics tools.

## Setup

```bash
pip install requests beautifulsoup4
```

## Run

```bash
python app.py
```
